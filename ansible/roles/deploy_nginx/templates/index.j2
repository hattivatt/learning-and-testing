<!doctype html>
<html>
  <head>
    <title>This is the title of the webpage!</title>
  </head>
  <body>
    <font color="green">Owner of this server is: {{ owner }}<br>
    <p>This are an example paragraph bla bla bla. Anything in the <strong>body</strong> tag will appear on the page, just like this <strong>p</strong> tag and its contents.</p>
    Server hostname is: {{ ansible_hostname }}<br>
    Server OS family is: {{ ansible_os_family }}<br>
    Server IP is: {{ ansible_default_ipv4.address }}<br>
  </body>
</html>
